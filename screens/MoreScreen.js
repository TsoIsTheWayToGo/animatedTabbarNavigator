import React, { useState, useReducer } from 'react';
import { Text, StyleSheet, View, FlatList, Button, TextInput, TouchableOpacity, SafeAreaView } from 'react-native';

const MoreScreen = () => {
	return (
		<SafeAreaView>
			<View style={{ alignItems: 'center', justifyContent: 'center', flex: 1 }}>
				<Text style={{ fontSize: 25 }}>Home/Explore screen</Text>
			</View>
		</SafeAreaView>
	);
};

const styles = StyleSheet.create({});

export default MoreScreen;
